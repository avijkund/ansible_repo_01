pipeline {
  agent { label 'linux' }
  environment {
   ANSIBLE_PRIVATE_KEY=credentials('client_key') 
  }
   stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook -i inventory --private-key=$ANSIBLE_PRIVATE_KEY  web.yml'
      }
    }
  }
}
