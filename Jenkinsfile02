pipeline {
  agent { label 'linux' }
  environment {
   ANSIBLE_PRIVATE_KEY=credentials('id_rsa.pub') 
  }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook -i inventory --private-key=$ANSIBLE_PRIVATE_KEY web.yml'
      }
    }
  }
}
