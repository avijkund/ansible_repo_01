pipeline {
  agent { label 'linux' }
  environment {
   ANSIBLE_PRIVATE_KEY=credentials('id_rsa.pub') 
  }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook web.yml'
      }
    }
  }
}
